#getApplication

# If there is an "application" entry then use its value. Otherwise if there
# is "solver" then use foamRun. Otherwise if there is "regionSolvers" then
# use foamMultiRun. Otherwise generate an error relating to the
# "application" entry being missing.
if foamDictionary -entry application -quiet system/controlDict
then
    foamDictionary -entry application -value system/controlDict
elif foamDictionary -entry solver -quiet system/controlDict
then
    echo foamRun
elif foamDictionary -entry regionSolvers -quiet system/controlDict
then
    echo foamMultiRun
else
    foamDictionary -entry application -value system/controlDict
fi